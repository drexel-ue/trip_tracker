import 'package:flutter/material.dart';
import 'package:trip_tracker/views/home.dart';

main() => runApp(
      MaterialApp(
        title: 'Trip Tracker',
        home: MyHomePage(),
      ),
    );
